{{ {
  "default_channel": sr_radio_default_channel,
  "port": sr_radio_api_port,
  "default_output": sr_radio_default_output,
  "outputs": sr_radio_outputs,
  "channels": sr_radio_channels
} | to_nice_json(sort_keys=False) }}
